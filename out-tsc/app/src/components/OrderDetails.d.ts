import type { Order } from '../types/types';
type Props = {
    order: Order;
};
declare const OrderDetails: ({ order }: Props) => import("react/jsx-runtime").JSX.Element;
export default OrderDetails;
